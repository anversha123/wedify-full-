services:
  - type: web
    name: wedify-backend
    env: python
    plan: free
    region: ohio
    rootDirectory: backend
    buildCommand: ./build.sh
    startCommand: gunicorn wedify_project.wsgi:application

    envVars:
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".render.com"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: wedify-db
          property: connectionString
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_PUBLISHABLE_KEY
        sync: false

databases:
  - name: wedify-db
    plan: free
    region: ohio
